// Intentionally left empty for now.
// We previously experimented with a dashboard-wide floating copilot,
// but the current product requirement is to keep floating chat only
// on marketplace/templates pages, so this component is disabled.
